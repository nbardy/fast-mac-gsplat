# STAR PRT v0 progress

2026-05-15:

- Documented implemented versus placeholder STAR PRT surfaces and static
  deterministic compact backward promotion gates.
- Extended the CPU-only smoke to import both `torch_gsplat_bridge_star_prt` and
  `research_project.trainer_harness.curve_tube`, validate documented tensor
  shapes, check static projection residuals, and compare dense harness rendering
  against bridge dense rendering.
- No `star_uvt_v0` files were edited by this docs/tests lane.
